<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infodart.instaproject">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera2.full" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.InstaHome"
            android:label="@string/title_activity_insta_home" />
        <activity android:name=".ui.BaseActivity" />
        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".ui.SignupActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.PhotoActivity"
            android:label="@string/title_activity_photo"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.ShareToActivity"
            android:label="@string/title_shareto" />
        <activity android:name=".ui.PostActivity" />
        <activity android:name=".ui.CommentsActivity" />
        <activity
            android:name=".ui.WallActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.ScrollingActivity"
            android:label="@string/title_activity_scrolling" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.CommentActivity" />
        <activity android:name=".ui.UsersActivity"></activity>
    </application>

</manifest>